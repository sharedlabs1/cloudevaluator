// .env.example

# Server Configuration
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:3001

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cloud_assessment
DB_USER=postgres
DB_PASSWORD=X9085565r@

# Redis Configuration
#REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=8h

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=Cloud Assessment Platform <noreply@assessment.com>

# Encryption Key (generate a random 32-byte key)
ENCRYPTION_KEY=your-32-byte-encryption-key-for-cloud-credentials

# Cloud Provider Settings
AWS_DEFAULT_REGION=us-east-1
AZURE_DEFAULT_REGION=eastus
GCP_DEFAULT_REGION=us-central1

# File Upload Limits
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=.pdf,.doc,.docx,.txt,.zip,.jpg,.jpeg,.png

# Logging
LOG_LEVEL=info

# Rate Limiting
API_RATE_LIMIT=100
AUTH_RATE_LIMIT=5
